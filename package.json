{
  "name": "blazor-starter",
  "version": "1.0.0",
  "scripts": {
    "start": "swa start http://localhost:5000 --run 'dotnet run --project Client/Client.csproj' --api-location Api"
  },
 "devDependencies": {
  },
  "dependencies": {
    "sirv-cli": "^2.0.2"
  }
}
